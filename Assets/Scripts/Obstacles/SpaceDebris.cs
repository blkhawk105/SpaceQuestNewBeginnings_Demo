using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceDebris : StationeryObstacles
{
    // Start is called before the first frame update
    void Start()
    {
        IsDestroyable = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
